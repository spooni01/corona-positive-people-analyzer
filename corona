#!/usr/bin/env bash

###############################################
# Name:     Corona - positive people analyzer #
# Author:   Adam Ližičiar, xlizic00           #
###############################################

################# Functions ###################


################### Main #######################
export POSIXLY_CORRECT=yes
USAGE_STRING=$(printf "usage:\n\t$0 [-h] [FILTERS] [COMMAND]")

# Help
if  [ "$1" = "-h" ] 
then
    echo "TODO: Napoveda"

# Write help
elif [ "$1" = "-a" ] && echo "$2" | grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" && [ "$2" != "" ] 
then
    echo "datetime format ok"
fi

exit 0